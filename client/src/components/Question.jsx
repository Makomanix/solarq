import React from 'react'

export default function Question({question}) {
    const { text, difficulty, points, catagory, answer, option1, option2, option3, option4, hint } = question
    return (
        <div>Question</div>
    )
}
